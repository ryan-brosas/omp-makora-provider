omp-o3l: Strip leaked GLM 5.1 chain-of-thought content markers from assistant messages
=====================================================================

STATUS: /create complete — PRD rewritten (was padded, now focused)

PHASE: /create

COMPLETED:
- [x] Restored accidentally deleted .pi/ files from working tree
- [x] Rewrote prd.md from 767-line padded version to focused ~80-line version
- [x] Confirmed prd.json is accurate (no changes needed)
- [x] Identified test failures: 4 of 10 tests fail because `runHook` returns hook's return value (undefined) instead of the mutated message

EXISTING IMPLEMENTATION (already in index.ts):
- `stripGlmCotMarkers()` at lines 635-662
- Integration in `message_end` handler at lines 711-724
- Both are correct and complete

KNOWN ISSUE:
- Test helper `runHook` at tests/glm-cot-strip.test.ts:50-53 returns hook() return value
- The hook mutates event.message in-place (line 767 comment confirms this)
- `runHook` should return `{ message }` after calling the hook, not the hook's return value
- Fix is a one-line change in the test helper

NEXT:
- /plan → /ship (fix test helper) → /verify → /review → /close
