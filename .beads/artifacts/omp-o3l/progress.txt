omp-o3l: Strip leaked GLM 5.1 chain-of-thought content markers from assistant messages
=====================================================================

STATUS: /ship implemented — ready for /verify

PHASE: /ship

COMPLETED ARTIFACTS:
- [x] Expanded `prd.md` in place to satisfy the user-required quality gate: 766 lines (minimum 600)
- [x] Expanded `plan.md` in place to satisfy the user-required quality gate: 800 lines (minimum 600)
- [x] Expanded `tasks.md` in place to satisfy the user-required quality gate: 1092 lines (minimum 400)
- [x] Preserved the existing PRD, plan, task, decision, and context-capsule scope for bead omp-o3l

IMPLEMENTATION COMPLETED:
- [x] Added GLM 5.1 CoT marker constants for ` thinking` and ` response`
- [x] Added `stripGlmCotMarkers()` helper near the existing control-token cleanup
- [x] Extracted leaked reasoning between complete ordered marker pairs
- [x] Moved extracted reasoning into `reasoning_content` on repaired messages
- [x] Appended leaked reasoning to an existing `reasoning_content` value instead of overwriting it
- [x] Replaced visible content with the post-` response` assistant answer
- [x] Preserved non-text content blocks while normalizing leaked text blocks
- [x] Left partial leaks unchanged as planned fallback behavior
- [x] Left marker pairs inside fenced code blocks unchanged to avoid corrupting legitimate text
- [x] Integrated stripping into the existing `message_end` hook before tool-call parsing
- [x] Kept behavior gated to `zai-org/GLM-5.1-FP8`
- [x] Preserved existing Kimi/Qwen/other model tool-call repair behavior
- [x] Preserved existing GLM tool-call repair after cleaned content is produced
- [x] Added unit coverage in `tests/glm-cot-strip.test.ts`

VERIFICATION COMPLETED:
- [x] `wc -l .beads/artifacts/omp-o3l/prd.md .beads/artifacts/omp-o3l/plan.md .beads/artifacts/omp-o3l/tasks.md` — PASS: 766 / 800 / 1092 lines
- [x] `npx vitest run tests/glm-cot-strip.test.ts --reporter verbose` — PASS: 10 tests passed
- [x] `npm run check` — PASS (`nothing to check`)
- [x] `npm run build` — PASS (`nothing to build`)
- [x] `br lint omp-o3l --json` — PASS: zero issues
- [x] `br dep cycles --json` — PASS: zero cycles
- [x] Placeholder/stub scan of `index.ts` and `tests/glm-cot-strip.test.ts` — PASS: no TODO/FIXME/stub/not-implemented matches in bead code; only pre-existing UI placeholder strings in login prompt code

VERIFICATION NOTES:
- `npx tsc --noEmit --skipLibCheck --allowImportingTsExtensions --module NodeNext --moduleResolution NodeNext --target ES2022 --types node index.ts tests/glm-cot-strip.test.ts` did not run because `typescript` is not installed in this project; `npx` reported that `tsc` is not available. No dependency was added because the package has no typecheck script and this bead does not scope dependency changes.
- A red/green check was performed for the existing-`reasoning_content` behavior: the new test first failed with `expected 'leaked reasoning' to be 'parser reasoning\nleaked reasoning'`, then passed after the hook appended existing reasoning.

NOTES:
- `br update --actor "${BR_ACTOR:-assistant}" omp-o3l --status in_progress --claim --json` reported the bead was already assigned to `ryan`; implementation proceeded on the in-progress bead without changing ownership.
- The broader working tree contained pre-existing unrelated `.pi/` deletions and untracked `.omp/` / `node_modules/` files before this /ship work began. They were not edited for this bead.
- `/review`, `/pr`, and `/close` were not run.

NEXT:
- Run /verify omp-o3l
