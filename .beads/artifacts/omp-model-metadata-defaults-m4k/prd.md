---
purpose: Product Requirements Document for a bead
updated: 2026-06-17
---

# PRD: Populate production model defaults — maxTokens, vision config, metadata gaps

**Bead:** omp-model-metadata-defaults-m4k | **Type:** task | **Priority:** P1
**Created:** 2026-06-17 | **Estimate:** 40 min

## Problem

WHEN pi sends a chat completion request THEN output token limits are unbounded BECAUSE all 10 Makora models in `models.json` have `maxTokens: 0`. Pi relies on `maxTokens` to cap `max_tokens`/`max_completion_tokens` in API requests — with zero, no cap is applied, and expensive reasoning models (DeepSeek V4 Pro, GLM 5.1, MiniMax M3) can burn thousands of thinking tokens with no guardrail.

Additionally, WHEN a user sends an image to a multimodal model (Kimi K2.6, Kimi K2.7, MiniMax M3) THEN pi doesn't know the image limits BECAUSE the `vision` config (`maxImagesPerRequest`) is missing from `patch.json`. Without explicit limits, pi may default to a conservative value or allow unbounded images, both of which degrade the user experience.

**Who is affected?** All users of all Makora models. Every chat completion is affected by `maxTokens: 0`. Multimodal users are affected by missing vision config.

**Why now?** The existing beads (omp-test-tool-call-parsers-8l6, omp-tool-call-repair-hardening-307) address tool call repair reliability but not model metadata. This bead fills the last major production-readiness gap: proper output caps and capability declarations. Without it, the provider is functional but not production-grade.

## Scope

### In Scope
- Research and set proper `maxTokens` values for all 10 models in `patch.json`
- Add `vision` config with `maxImagesPerRequest` for Kimi K2.6, Kimi K2.7, and MiniMax M3
- Verify `contextWindow` values in `models.json` match known model specs; override in `patch.json` where incorrect
- Ensure `custom-models.json` Llama 3.3 70B FP8 has consistent metadata with other models
- Update `update-models.js` to set a sensible `maxTokens` default (8192) instead of `0`
- Update README model table notes to reflect maxTokens and vision capabilities
- Add `maxTokens` to the `patch.json` schema documentation in README

### Out of Scope
- Live API testing (requires `MAKORA_OPTIMIZE_TOKEN`)
- Changing model pricing (Makora doesn't expose pricing)
- Adding new models
- Modifying the tool call repair pipeline
- Adding streaming configuration
- Changing reasoning/thinkingFormat values

## Requirements

| # | Requirement | Priority | Acceptance Criteria |
|---|------------|----------|-------------------|
| 1 | All models have non-zero maxTokens | MUST | Every model in the merged model list (models.json → patch.json → custom-models.json) has `maxTokens > 0` |
| 2 | maxTokens values are model-appropriate | MUST | Reasoning models (DeepSeek V4, GLM 5.1, Kimi, Qwen, MiniMax M3, GPT-OSS) have `maxTokens >= 16384`; non-reasoning models (Llama 3.3) have `maxTokens >= 8192` |
| 3 | DeepSeek V4 models have 32768 maxTokens | MUST | DeepSeek V4 Flash and Pro support 32K output natively |
| 4 | Multimodal models have vision.maxImagesPerRequest | MUST | Kimi K2.6, Kimi K2.7, MiniMax M3 have `vision: { maxImagesPerRequest: 5 }` in patch.json |
| 5 | update-models.js defaults to maxTokens: 8192 | MUST | `transformApiModel` sets `maxTokens: 8192` instead of `0` |
| 6 | contextWindow values validated | SHOULD | GLM 5.1 contextWindow is 200000 (patched), not 202752 (API); other models verified against known specs |
| 7 | README reflects metadata changes | SHOULD | Model table notes include maxTokens and vision info where relevant |
| 8 | No regression in provider startup | MUST | Provider starts successfully; `npx tsc --noEmit index.ts` passes |
| 9 | Existing patch.json entries preserved | MUST | No existing patch entries (reasoning, thinkingLevelMap, compat, notes, input) are removed or altered |

## Technical Context

**Key files:**
- `patch.json` — Add `maxTokens` and `vision` overrides for all models
- `models.json` — Auto-generated; do NOT edit directly
- `scripts/update-models.js` — Change `maxTokens: 0` → `maxTokens: 8192` default
- `README.md` — Update model table (regenerated by update-models.js)
- `index.ts` — No changes needed (maxTokens is passive metadata)

**Model maxTokens research:**

| Model | Known Max Output | Recommended maxTokens | Source |
|-------|-----------------|----------------------|--------|
| DeepSeek V4 Flash | 32K | 32768 | DeepSeek API docs; `max_tokens` range 1-32768 |
| DeepSeek V4 Pro | 32K | 32768 | DeepSeek API docs |
| GLM 5.1 FP8 | 8K-32K | 16384 | GLM-4 supports up to 4096; GLM-5 is higher but conservative default for vLLM |
| GPT-OSS 120B | Unknown | 16384 | Comparable to GPT-4 class; conservative default |
| Kimi K2.6 NVFP4 | 8K+ | 16384 | Kimi K2 supports long output; 16K conservative for reasoning |
| Kimi K2.7 Code | 8K+ | 16384 | Same family as K2.6 |
| Llama 3.3 70B | 8K | 8192 | Meta's recommended default |
| Llama 3.3 70B FP8 | 16K (already set) | 16384 | Already in custom-models.json |
| MiniMax M3 MXFP8 | Unknown | 16384 | Conservative default for reasoning model |
| Qwen 3.6 27B NVFP4 | 8K+ | 16384 | Qwen supports 8192+; 16K for reasoning overhead |
| Qwen 3.6 35B A3B NVFP4 | 8K+ | 16384 | Same family as 27B |

**Vision config format (from JsonModel interface in index.ts):**
```typescript
vision?: {
  maxImagesPerRequest?: number;
};
```

**Current multimodal models (from patch.json `input` field):**
- `nvidia/Kimi-K2.6-NVFP4`: `input: ["text", "image"]`
- `moonshotai/Kimi-K2.7-Code`: `input: ["text", "image"]`
- `MiniMaxAI/MiniMax-M3-MXFP8`: `input: ["text", "image"]`

**Data flow:**
```
models.json (API: maxTokens: 0)  →  patch.json (override: maxTokens)  →  merged model list
models.json (API: input: [text])  →  patch.json (override: input)        →  merged model list
```

## Approach

1. **maxTokens via patch.json**: Add `maxTokens` to each model's patch entry in `patch.json`. This is the correct layer because `models.json` is auto-generated from the API (which doesn't provide max output tokens) and `patch.json` is the manual override layer.

2. **vision via patch.json**: Add `vision: { maxImagesPerRequest: 5 }` to the three multimodal models' patch entries. Value of 5 is standard for vLLM deployments and matches common defaults.

3. **update-models.js default**: Change the hardcoded `maxTokens: 0` to `maxTokens: 8192` in `transformApiModel`. This is a sensible fallback that gets overridden by `patch.json` for specific models. It's safer than 0.

4. **contextWindow validation**: GLM 5.1's `contextWindow` from the API is 202752 but should be 200000. This is already patched. No other corrections needed based on known specs.

**Alternatives considered:**
- **Set maxTokens in models.json directly** — Rejected. models.json is auto-generated and would be overwritten by `update-models.js`.
- **Set maxTokens in update-models.js per-model** — Rejected. The script doesn't have per-model knowledge; that's patch.json's job.
- **Use 0 as "unlimited" and let pi handle it** — Rejected. Pi uses maxTokens to set `max_tokens`/`max_completion_tokens` in API requests. 0 means no limit, which is dangerous for reasoning models.
- **Set all models to 4096** — Rejected. Too restrictive for reasoning models that need thinking token budget.

## Risks

| Risk | Likelihood | Impact | Mitigation |
|------|-----------|--------|------------|
| maxTokens too restrictive for some use cases | Low | Medium | Values are defaults; pi may allow per-request override. 16K+ for reasoning models provides ample budget. |
| maxTokens too permissive (cost) | Low | Low | Values are caps, not targets. Models only use what they need. |
| vision.maxImagesPerRequest too low | Low | Low | 5 images is generous for coding workflows; can increase later. |
| API changes model capabilities | Low | Medium | patch.json is the manual override layer; can be updated independently. |
| update-models.js default conflicts with patch.json | None | N/A | patch.json always wins (applyPatch runs after model construction). |

## Acceptance Criteria

- [ ] AC-1: All upstream models in the merged model list have `maxTokens > 0` after applying `patch.json`.
- [ ] AC-2: The custom FP8 Llama model keeps its existing non-zero `maxTokens` value.
- [ ] AC-3: DeepSeek V4 Flash and DeepSeek V4 Pro resolve to `maxTokens: 32768`.
- [ ] AC-4: All reasoning models resolve to `maxTokens >= 16384`.
- [ ] AC-5: Kimi K2.6, Kimi K2.7, and MiniMax M3 resolve to `vision.maxImagesPerRequest: 5`.
- [ ] AC-6: `scripts/update-models.js` emits `maxTokens: 8192` as the default for future API-discovered models.
- [ ] AC-7: `applyPatch` in both runtime provider and update script preserves and applies `vision` metadata.
- [ ] AC-8: README model table and patch metadata docs describe maxTokens and vision fields.
- [ ] AC-9: Existing reasoning, thinkingLevelMap, compat, notes, and input declarations are preserved.
- [ ] AC-10: Local JSON, merged metadata, and TypeScript verification commands pass or blockers are explicitly reported.

## Success Criteria

- [ ] All 10 models in merged model list have `maxTokens > 0`
    - Verify: inspect merged output or run `node -e "require('./models.json')"` then check patch.json application
- [ ] DeepSeek V4 Flash and Pro have `maxTokens: 32768`
    - Verify: grep `maxTokens` in patch.json for DS model IDs
- [ ] Kimi K2.6, K2.7, MiniMax M3 have `vision.maxImagesPerRequest: 5`
    - Verify: grep `maxImagesPerRequest` in patch.json
- [ ] `update-models.js` `transformApiModel` sets `maxTokens: 8192`
    - Verify: grep `maxTokens: 8192` in scripts/update-models.js
- [ ] Provider starts without errors
    - Verify: `npx tsc --noEmit index.ts` passes
- [ ] README table reflects changes
    - Verify: run `node scripts/update-models.js` (if API key available) or manually verify table accuracy
- [ ] No existing patch.json entries removed
    - Verify: git diff shows only additions, no deletions from existing patch entries

## Expanded Requirements Ledger
### ER-01: DeepSeek V4 Flash
- Model ID: `deepseek-ai/DeepSeek-V4-Flash`.
- Required output cap: `maxTokens: 32768`.
- Required input declaration: `text`.
- Reasoning contract: reasoning field.
- Product risk addressed: DeepSeek output cap is highest risk because the model can think for long spans.
- Acceptance evidence: merged provider metadata exposes the listed cap after `models.json` is patched by `patch.json` or supplied by `custom-models.json`.
- Non-regression rule: existing `notes`, `thinkingLevelMap`, `compat`, and input semantics remain present unless this PRD explicitly names a change.
- User-visible outcome: pi sends bounded requests and can advertise the correct capability without relying on an implicit provider default.
- Failure mode prevented: a request proceeds with `maxTokens: 0`, causing no explicit max token parameter to be sent to Makora.
- Verification owner: the ship phase validates the JSON and inspects the merged metadata using a local script.
### ER-02: DeepSeek V4 Pro
- Model ID: `deepseek-ai/DeepSeek-V4-Pro`.
- Required output cap: `maxTokens: 32768`.
- Required input declaration: `text`.
- Reasoning contract: reasoning_content field.
- Product risk addressed: Pro is expensive and must receive the full native cap, not an unlimited request.
- Acceptance evidence: merged provider metadata exposes the listed cap after `models.json` is patched by `patch.json` or supplied by `custom-models.json`.
- Non-regression rule: existing `notes`, `thinkingLevelMap`, `compat`, and input semantics remain present unless this PRD explicitly names a change.
- User-visible outcome: pi sends bounded requests and can advertise the correct capability without relying on an implicit provider default.
- Failure mode prevented: a request proceeds with `maxTokens: 0`, causing no explicit max token parameter to be sent to Makora.
- Verification owner: the ship phase validates the JSON and inspects the merged metadata using a local script.
### ER-03: GLM 5.1 FP8
- Model ID: `zai-org/GLM-5.1-FP8`.
- Required output cap: `maxTokens: 16384`.
- Required input declaration: `text`.
- Reasoning contract: reasoning_content field.
- Product risk addressed: GLM also has a patched 200000 context window that must remain intact.
- Acceptance evidence: merged provider metadata exposes the listed cap after `models.json` is patched by `patch.json` or supplied by `custom-models.json`.
- Non-regression rule: existing `notes`, `thinkingLevelMap`, `compat`, and input semantics remain present unless this PRD explicitly names a change.
- User-visible outcome: pi sends bounded requests and can advertise the correct capability without relying on an implicit provider default.
- Failure mode prevented: a request proceeds with `maxTokens: 0`, causing no explicit max token parameter to be sent to Makora.
- Verification owner: the ship phase validates the JSON and inspects the merged metadata using a local script.
### ER-04: GPT-OSS 120B
- Model ID: `openai/gpt-oss-120b`.
- Required output cap: `maxTokens: 16384`.
- Required input declaration: `text`.
- Reasoning contract: reasoning field.
- Product risk addressed: The model reasons by default, so a non-zero token ceiling is required.
- Acceptance evidence: merged provider metadata exposes the listed cap after `models.json` is patched by `patch.json` or supplied by `custom-models.json`.
- Non-regression rule: existing `notes`, `thinkingLevelMap`, `compat`, and input semantics remain present unless this PRD explicitly names a change.
- User-visible outcome: pi sends bounded requests and can advertise the correct capability without relying on an implicit provider default.
- Failure mode prevented: a request proceeds with `maxTokens: 0`, causing no explicit max token parameter to be sent to Makora.
- Verification owner: the ship phase validates the JSON and inspects the merged metadata using a local script.
### ER-05: Kimi K2.6 NVFP4
- Model ID: `nvidia/Kimi-K2.6-NVFP4`.
- Required output cap: `maxTokens: 16384`.
- Required input declaration: `text+image`.
- Reasoning contract: reasoning field.
- Product risk addressed: This multimodal model needs both output caps and a bounded image count.
- Acceptance evidence: merged provider metadata exposes the listed cap after `models.json` is patched by `patch.json` or supplied by `custom-models.json`.
- Non-regression rule: existing `notes`, `thinkingLevelMap`, `compat`, and input semantics remain present unless this PRD explicitly names a change.
- User-visible outcome: pi sends bounded requests and can advertise the correct capability without relying on an implicit provider default.
- Failure mode prevented: a request proceeds with `maxTokens: 0`, causing no explicit max token parameter to be sent to Makora.
- Verification owner: the ship phase validates the JSON and inspects the merged metadata using a local script.
### ER-06: Kimi K2.7 Code
- Model ID: `moonshotai/Kimi-K2.7-Code`.
- Required output cap: `maxTokens: 16384`.
- Required input declaration: `text+image`.
- Reasoning contract: reasoning field.
- Product risk addressed: This code-oriented Kimi model shares the same visual request contract.
- Acceptance evidence: merged provider metadata exposes the listed cap after `models.json` is patched by `patch.json` or supplied by `custom-models.json`.
- Non-regression rule: existing `notes`, `thinkingLevelMap`, `compat`, and input semantics remain present unless this PRD explicitly names a change.
- User-visible outcome: pi sends bounded requests and can advertise the correct capability without relying on an implicit provider default.
- Failure mode prevented: a request proceeds with `maxTokens: 0`, causing no explicit max token parameter to be sent to Makora.
- Verification owner: the ship phase validates the JSON and inspects the merged metadata using a local script.
### ER-07: Qwen 3.6 27B NVFP4
- Model ID: `unsloth/Qwen3.6-27B-NVFP4`.
- Required output cap: `maxTokens: 16384`.
- Required input declaration: `text`.
- Reasoning contract: reasoning field.
- Product risk addressed: Qwen thinking can consume budget quickly if maxTokens stays zero.
- Acceptance evidence: merged provider metadata exposes the listed cap after `models.json` is patched by `patch.json` or supplied by `custom-models.json`.
- Non-regression rule: existing `notes`, `thinkingLevelMap`, `compat`, and input semantics remain present unless this PRD explicitly names a change.
- User-visible outcome: pi sends bounded requests and can advertise the correct capability without relying on an implicit provider default.
- Failure mode prevented: a request proceeds with `maxTokens: 0`, causing no explicit max token parameter to be sent to Makora.
- Verification owner: the ship phase validates the JSON and inspects the merged metadata using a local script.
### ER-08: Qwen 3.6 35B A3B NVFP4
- Model ID: `unsloth/Qwen3.6-35B-A3B-NVFP4`.
- Required output cap: `maxTokens: 16384`.
- Required input declaration: `text`.
- Reasoning contract: reasoning field.
- Product risk addressed: The larger Qwen variant must match the 27B metadata pattern.
- Acceptance evidence: merged provider metadata exposes the listed cap after `models.json` is patched by `patch.json` or supplied by `custom-models.json`.
- Non-regression rule: existing `notes`, `thinkingLevelMap`, `compat`, and input semantics remain present unless this PRD explicitly names a change.
- User-visible outcome: pi sends bounded requests and can advertise the correct capability without relying on an implicit provider default.
- Failure mode prevented: a request proceeds with `maxTokens: 0`, causing no explicit max token parameter to be sent to Makora.
- Verification owner: the ship phase validates the JSON and inspects the merged metadata using a local script.
### ER-09: MiniMax M3 MXFP8
- Model ID: `MiniMaxAI/MiniMax-M3-MXFP8`.
- Required output cap: `maxTokens: 16384`.
- Required input declaration: `text+image`.
- Reasoning contract: reasoning_content field.
- Product risk addressed: MiniMax combines reasoning and images, so it has both metadata additions.
- Acceptance evidence: merged provider metadata exposes the listed cap after `models.json` is patched by `patch.json` or supplied by `custom-models.json`.
- Non-regression rule: existing `notes`, `thinkingLevelMap`, `compat`, and input semantics remain present unless this PRD explicitly names a change.
- User-visible outcome: pi sends bounded requests and can advertise the correct capability without relying on an implicit provider default.
- Failure mode prevented: a request proceeds with `maxTokens: 0`, causing no explicit max token parameter to be sent to Makora.
- Verification owner: the ship phase validates the JSON and inspects the merged metadata using a local script.
### ER-10: Llama 3.3 70B Instruct
- Model ID: `meta-llama/Llama-3.3-70B-Instruct`.
- Required output cap: `maxTokens: 8192`.
- Required input declaration: `text`.
- Reasoning contract: no reasoning.
- Product risk addressed: This non-reasoning model receives the conservative fallback cap.
- Acceptance evidence: merged provider metadata exposes the listed cap after `models.json` is patched by `patch.json` or supplied by `custom-models.json`.
- Non-regression rule: existing `notes`, `thinkingLevelMap`, `compat`, and input semantics remain present unless this PRD explicitly names a change.
- User-visible outcome: pi sends bounded requests and can advertise the correct capability without relying on an implicit provider default.
- Failure mode prevented: a request proceeds with `maxTokens: 0`, causing no explicit max token parameter to be sent to Makora.
- Verification owner: the ship phase validates the JSON and inspects the merged metadata using a local script.
### ER-11: Llama 3.3 70B FP8
- Model ID: `amd/Llama-3.3-70B-Instruct-FP8-KV`.
- Required output cap: `maxTokens: 16384`.
- Required input declaration: `text`.
- Reasoning contract: no reasoning.
- Product risk addressed: The custom model already has a non-zero cap and must not regress.
- Acceptance evidence: merged provider metadata exposes the listed cap after `models.json` is patched by `patch.json` or supplied by `custom-models.json`.
- Non-regression rule: existing `notes`, `thinkingLevelMap`, `compat`, and input semantics remain present unless this PRD explicitly names a change.
- User-visible outcome: pi sends bounded requests and can advertise the correct capability without relying on an implicit provider default.
- Failure mode prevented: a request proceeds with `maxTokens: 0`, causing no explicit max token parameter to be sent to Makora.
- Verification owner: the ship phase validates the JSON and inspects the merged metadata using a local script.

## Acceptance Matrix Details
- AM-001 [vision]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-002 [merge]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-003 [script]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-004 [docs]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-005 [regression]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-006 [cap]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-007 [vision]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-008 [merge]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-009 [script]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-010 [docs]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-011 [regression]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-012 [cap]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-013 [vision]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-014 [merge]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-015 [script]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-016 [docs]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-017 [regression]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-018 [cap]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-019 [vision]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-020 [merge]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-021 [script]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-022 [docs]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-023 [regression]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-024 [cap]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-025 [vision]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-026 [merge]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-027 [script]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-028 [docs]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-029 [regression]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-030 [cap]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-031 [vision]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-032 [merge]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-033 [script]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-034 [docs]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-035 [regression]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-036 [cap]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-037 [vision]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-038 [merge]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-039 [script]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-040 [docs]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-041 [regression]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-042 [cap]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-043 [vision]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-044 [merge]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-045 [script]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-046 [docs]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-047 [regression]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-048 [cap]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-049 [vision]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-050 [merge]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-051 [script]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-052 [docs]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-053 [regression]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-054 [cap]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-055 [vision]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-056 [merge]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-057 [script]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-058 [docs]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-059 [regression]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-060 [cap]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-061 [vision]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-062 [merge]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-063 [script]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-064 [docs]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-065 [regression]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-066 [cap]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-067 [vision]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-068 [merge]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-069 [script]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-070 [docs]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-071 [regression]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-072 [cap]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-073 [vision]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-074 [merge]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-075 [script]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-076 [docs]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-077 [regression]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-078 [cap]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-079 [vision]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-080 [merge]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-081 [script]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-082 [docs]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-083 [regression]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-084 [cap]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-085 [vision]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-086 [merge]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-087 [script]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-088 [docs]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-089 [regression]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-090 [cap]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-091 [vision]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-092 [merge]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-093 [script]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-094 [docs]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-095 [regression]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-096 [cap]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-097 [vision]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-098 [merge]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-099 [script]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-100 [docs]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-101 [regression]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-102 [cap]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-103 [vision]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-104 [merge]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-105 [script]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-106 [docs]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-107 [regression]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-108 [cap]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-109 [vision]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-110 [merge]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-111 [script]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-112 [docs]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-113 [regression]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-114 [cap]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-115 [vision]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-116 [merge]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-117 [script]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-118 [docs]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-119 [regression]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.
- AM-120 [cap]: The implementation preserves production safety by ensuring every model record has deterministic, non-zero metadata at load time and by proving that no existing patch semantics are removed.

## Edge Cases and Negative Requirements
- NEG-001: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-002: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-003: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-004: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-005: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-006: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-007: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-008: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-009: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-010: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-011: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-012: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-013: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-014: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-015: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-016: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-017: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-018: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-019: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-020: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-021: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-022: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-023: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-024: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-025: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-026: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-027: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-028: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-029: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-030: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-031: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-032: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-033: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-034: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-035: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-036: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-037: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-038: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-039: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-040: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-041: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-042: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-043: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-044: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-045: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-046: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-047: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-048: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-049: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-050: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-051: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-052: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-053: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-054: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-055: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-056: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-057: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-058: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-059: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-060: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-061: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-062: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-063: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-064: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-065: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-066: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-067: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-068: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-069: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-070: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-071: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-072: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-073: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-074: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-075: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-076: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-077: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-078: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-079: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-080: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-081: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-082: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-083: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-084: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-085: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-086: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-087: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-088: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-089: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-090: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-091: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-092: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-093: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-094: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-095: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-096: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-097: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-098: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-099: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-100: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-101: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-102: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-103: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-104: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-105: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-106: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-107: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-108: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-109: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-110: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-111: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-112: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-113: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-114: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-115: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-116: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-117: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-118: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-119: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-120: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-121: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-122: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-123: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-124: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-125: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-126: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-127: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-128: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-129: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-130: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-131: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-132: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-133: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-134: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-135: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-136: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-137: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-138: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-139: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.
- NEG-140: Do not treat missing Makora API fields as a license to leave local metadata empty; when discovery omits output limits, the provider must supply conservative local defaults through patch or custom metadata.

## Additional Operational Acceptance Detail
- OP-001: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-002: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-003: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-004: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-005: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-006: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-007: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-008: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-009: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-010: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-011: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-012: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-013: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-014: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-015: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-016: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-017: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-018: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-019: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-020: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-021: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-022: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-023: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-024: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-025: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-026: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-027: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-028: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-029: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-030: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-031: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-032: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-033: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-034: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-035: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-036: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-037: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-038: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-039: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-040: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-041: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-042: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-043: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-044: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-045: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-046: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-047: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-048: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-049: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-050: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-051: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-052: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-053: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-054: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-055: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-056: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-057: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-058: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-059: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-060: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-061: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-062: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-063: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-064: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-065: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-066: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-067: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-068: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-069: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-070: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-071: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-072: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-073: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-074: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-075: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-076: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-077: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-078: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-079: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-080: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-081: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-082: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-083: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-084: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-085: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-086: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-087: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-088: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-089: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-090: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-091: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-092: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-093: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-094: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-095: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-096: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-097: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-098: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-099: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-100: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-101: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-102: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-103: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-104: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-105: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-106: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-107: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-108: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-109: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
- OP-110: During ship, verify that metadata defaults are explicit, reviewable, deterministic, and compatible with regenerated discovery output so future model refreshes cannot silently reintroduce an unbounded token ceiling.
